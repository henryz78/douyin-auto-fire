# 多账号模式下，每个账号一个独立环境文件（复制为 .env.account1、.env.account2 ...）
# 注意：任务配置路径用 TASK_CONFIG（不是 DOUYIN_CONFIG，那是 GitHub Actions 的 Secret 名）。
DOUYIN_COOKIE=账号1的CookieJSON
TASK_CONFIG=config/tasks/account1.json
# ARTIFACTS_DIR 不写也可以，程序默认使用 artifacts/<账号id>/
# ARTIFACTS_DIR=artifacts/account1
# 需要独立登录状态文件时再指定（文件不存在时回退到 DOUYIN_COOKIE）
# DOUYIN_STORAGE_STATE=storage_state/account1.json
# 固定代理（可选；HTTP 支持认证，SOCKS5 仅支持无认证）
# 通常建议作为全局环境变量配置，多账号可共用同一出口 IP。
# DOUYIN_PROXY_SERVER=http://proxy.example.com:3128
# DOUYIN_PROXY_USERNAME=proxy-user
# DOUYIN_PROXY_PASSWORD=proxy-password
# 钉钉通知（可选，每个账号可以配置不同的机器人）
# DINGTALK_WEBHOOK=
# DINGTALK_SECRET=
